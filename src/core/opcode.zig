pub const Opcode = enum(u8) {
    NOP = 0x00,
    HALT,
    JMP,
    BR,
    BR_IF,
    CALL,
    RET,
    CALL_REG = 0x0F,

    IADD = 0x10,
    ISUB,
    IMUL,
    IDIV,
    IMOD,
    IAND,
    IOR,
    IXOR,
    ISHL,
    ISHR,

    LOAD = 0x20,
    STORE,
    MEM_COPY,
    MEM_ZERO,
    HEAP_ALLOC,
    HEAP_FREE,
    PTR_ADD,
    PTR_SUB,

    ICMP_EQ = 0x30,
    ICMP_NE,
    ICMP_LT,
    ICMP_GT,
    ICMP_LE,
    ICMP_GE,

    TIME_NOW = 0x40,
    SLEEP_NS,
    THREAD_SPAWN,
    THREAD_JOIN,
    THREAD_YIELD,

    FS_OPEN = 0x48,
    FS_READ,
    FS_WRITE,
    FS_CLOSE,
    FS_SIZE,
    FS_SEEK,
    FS_MKDIR,
    FS_REMOVE,

    LOAD_MODULE = 0x50,
    STDIN_READ,
    STDOUT_WRITE,
    STDERR_WRITE,

    NET_OPEN = 0x54,
    NET_CLOSE,
    NET_SEND,
    NET_RECV,
    NET_POLL,
    NET_LISTEN,
    NET_ACCEPT,

    V_LOAD = 0x60,
    V_STORE,
    V_ADD,
    V_SUB,
    V_MUL,
    V_AND,
    V_OR,
    V_XOR,
    V_FADD,
    V_FSUB,
    V_FMUL,
    V_FDIV,
    V_FSQRT,
    V_SPLAT,
    V_SHUFFLE,

    ATOMIC_LOAD = 0x70,
    ATOMIC_STORE,
    ATOMIC_RMW,
    ATOMIC_CAS,

    //=============================
    // Floating Point (F64)
    //=============================
    FADD = 0x80,
    FSUB,
    FMUL,
    FDIV,
    FNEG,
    FABS,
    FSQRT,

    FCMP_EQ = 0x90,
    FCMP_NE,
    FCMP_LT,
    FCMP_GT,
    FCMP_LE,
    FCMP_GE,

    FCONV_I2F = 0xA0,
    FCONV_F2I,

    DL_OPEN = 0xB0,
    DL_SYM,
    DL_CALL,
    DL_CLOSE,
};
